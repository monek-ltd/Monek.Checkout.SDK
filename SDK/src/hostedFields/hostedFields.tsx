import React from 'react'
import ReactDOM from 'react-dom/client'
import HostedFieldsApp from './components/HostedFieldsApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HostedFieldsApp />
  </React.StrictMode>
)
