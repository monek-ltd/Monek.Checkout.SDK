import React from 'react'
import ReactDOM from 'react-dom/client'
import ExpressCheckoutApp from './components/ExpressCheckoutApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ExpressCheckoutApp />
  </React.StrictMode>
)
